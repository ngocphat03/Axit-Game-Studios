# Axit Registry

Framework-level registries live here when they are genuinely shared by the reusable base.

Concrete game projects should create `.axit/registry/architecture.yaml` from `.axit/templates/architecture.yaml` and own their project-specific state ownership, interfaces, API decisions, budgets, and forbidden patterns there.
