# Core Skills

Reserved for reviewed Axit Core Skills.

No legacy Game Studios skill has been migrated here yet.

Each accepted skill will use the Codex-compatible directory form:

```text
.axit/core/skills/<skill-id>/SKILL.md
```

Only reviewed Core skills are exposed through `.agents/skills/` for Codex discovery.
